from django.apps import AppConfig

# The class's path must be added to our projects(Libraries\Django\CoreySchafer_tutorial)
# setup.py file, inside the INSTALLED_APPS list.
class BlogConfig(AppConfig):
    name = 'blog'
